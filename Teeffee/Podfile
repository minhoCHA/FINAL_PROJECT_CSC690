# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'coffeemeetsboba' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for coffeemeetsboba

  target 'coffeemeetsbobaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'coffeemeetsbobaUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod 'TTFortuneWheel'
  pod 'CDYelpFusionKit', '1.5.1'
  pod 'AFNetworking', '~> 2.5'
  pod 'BDBOAuth1Manager'
  pod 'Kingfisher', '~> 5.0'
  pod 'Chatto', '= 3.3.1'
  pod 'ChattoAdditions', '= 3.3.1' # if you want to use the cells or the input component

end
